#!/bin/bash
# Simple bash setup script to configuration files into root

echo Copying configuration files to /root
cp vimrc.vim /root/.vimrc
cp rbashrc /root/.bashrc
cp bash_profile /root/.bash_profile
